/**
 * Run upon load of lgui
 * Loads in canvas and 
 */

let canvas = document.getElementById("canvas");
let ctx = canvas.getContext("2d");

/**
 * Draws the grid of the 
 */
function drawGrid() {

}